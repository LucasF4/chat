<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <%- include('../partials/links-head.ejs'); %>
  </head>
  <body class="">
    <%if(erro){%>
    <div class="bg-red-900 text-white text-center p-5 absolute w-full" role="alert"><%=erro%></div>
    <%}%>

    <div class="w-full h-screen flex items-center justify-center">
      <form
        class="flex flex-col gap-10 border border-solid border-[#ccc] rounded-xl w-[400px] p-5 shadow-lg"
        action="/login"
        method="post"
      >
        <div class="flex flex-col gap-2 text-center">
          <h1 class="text-3xl font-bold">Login</h1>
          <p class="text-[#575757]">Insira suas credenciais e faça o login.</p>
        </div>
        <div class="flex flex-col gap-2">
          <label>E-mail</label>
          <input
            class="border border-solid border-[#ccc] p-3 rounded-lg text-sm"
            name="email"
            type="text"
            placeholder="teste@email.com"
            required
          />
          <label>Senha</label>
          <input
            class="border border-solid border-[#ccc] p-3 rounded-lg text-sm"
            name="senha"
            type="password"
            placeholder="********"
            required
          />
          <a
            class="text-sm w-fit h-[20px] transition-all duration-300 hover:border-b hover:border-solid hover:border-black"
            href="/cadastro"
            >Não tem uma conta? Cadastre-se</a
          >
        </div>
        <button class="bg-black text-white p-3 rounded-xl mt-3" type="submit">
          Entrar
        </button>
      </form>
    </div>
  </body>
</html>
